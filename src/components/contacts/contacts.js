import React from 'react';

import style from './contacts.module.css';

const Contacts = () => {
	return (
		<main className={style.contacts}>
			<div className={style.container}>

			</div>
		</main>
	);
};

export default Contacts;
